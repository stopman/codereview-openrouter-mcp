{
  "mcpServers": {
    "codereview": {
      "command": "uv",
      "args": ["run", "codereview-openrouter-mcp"],
      "env": {
        "OPENROUTER_API_KEY": "${OPENROUTER_API_KEY}"
      }
    }
  }
}

{
  "mcpServers": {
    "planreview": {
      "command": "uv",
      "args": ["run", "planreview-openrouter-mcp"],
      "env": {
        "OPENROUTER_API_KEY": "${OPENROUTER_API_KEY}"
      }
    }
  }
}
